## Context {#context}

This design note depends on the meta extension specification. It references requirement `meta-extension-spec#REQ-MEM-1` and the shared term `term:file-memory` instead of copying their text.

## Design requirements {#design-requirements}

```docs-record
id = "REQ-DESIGN-1"
type = "requirement"

A dependent document MUST be able to reference a stored requirement without restating it.
```

```docs-link
from = "#REQ-DESIGN-1"
to = "meta-extension-spec#REQ-MEM-1"
type = "references"
```

```docs-link
from = "#REQ-DESIGN-1"
to = "term:file-memory"
type = "references"
```
